# 贡献指南

感谢你愿意参与 text2link 的开发！请先阅读本指南，确保贡献过程顺畅。

## 开发环境

- 一个 Chromium 内核浏览器（Chrome / Edge）
- Tampermonkey 或 Violentmonkey（用于安装开发版脚本）
- Node.js（可选，用于端到端验证）

## 开发流程

1. **Fork 并检出**仓库，创建功能分支（如 `feat/xxx` 或 `fix/xxx`）。
2. **修改** `text2link.user.js`：
   - 保持纯原生 JavaScript、`'use strict'`；仅使用黑名单所需的 GM 存储 / 菜单 API（`GM_getValue`、`GM_setValue`、`GM_registerMenuCommand`）；
   - 新增类名 / 常量使用 `sla-` 命名空间，避免与页面样式冲突；
   - 关键逻辑添加中文注释，说明「为什么这么做」（尤其是边界情况和浏览器兼容性处理）；
   - 功能变更时同步更新元数据中的 `@version` 与 `@description`，并在 `CHANGELOG.md` 增加条目。
3. **手动验证**：在浏览器中打开 `test-page.html`，确认：
   - 全部用例（六类链接、负例、防误伤）转换正确、文本一字不差；
   - 悬停高亮、单击新标签页、拖选不跳转可复制均正常；
   - 点击「切换到深色模式」后配色正确切换并随页面主题变化。
4. **端到端验证（可选但推荐）**：用 Playwright + 真实 Chromium 跑一遍自动化检查，覆盖转换、交互、主题、动态内容与重复转换等场景；新功能必须补充对应断言。
5. **提交**：提交信息用简短中文或英文描述变更，如 `feat: 支持邮箱链接`、`fix: 修复 TreeWalker 遍历中断`。
6. **发起 Pull Request**：描述变更内容、验证方式与已知影响；若改动涉及行为变化，请附上截图或测试输出。

## 代码风格

- 使用 `const` / `let`，避免 `var`；启用 `'use strict'`。
- 函数与变量命名使用驼峰式，常量使用大写蛇形。
- 注释用中文，解释意图与边界条件，不写废话注释。
- 不引入任何外部依赖、构建工具或压缩产物。

## 行为变更须知

- 修改识别正则时，务必同步更新 `test-page.html` 中的正例与负例，防止回归。
- 修改点击 / 选区拦截逻辑时，需同时验证 Chromium 与 Firefox 的行为差异（如链接拖拽与选区产生的差异）。
- 禁止添加收集用户数据、发起额外网络请求或执行页面之外副作用的代码。

## 提问与讨论

功能建议、Bug 报告请先使用 GitHub Issues；不确定的设计问题可以在 Issue 中讨论后再动手实现。
