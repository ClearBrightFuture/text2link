# 安全说明

## 安全承诺

text2link 是一个零依赖、零网络请求的用户脚本：

- 仅使用脚本管理器提供的本地存储与菜单 API（`GM_getValue` / `GM_setValue` / `GM_registerMenuCommand`），不申请任何浏览器级权限；
- 不收集、不上传任何用户数据；
- 不引入任何外部依赖，降低供应链风险；
- 只处理页面中已是纯文本的链接并转换为可点击链接，不修改页面其他行为；
- 脚本自身不会发起任何网络请求——只有在用户主动点击链接后，浏览器才会按协议打开目标。
- 黑名单数据只保存在本机脚本管理器的存储中，不会同步到任何服务器。

## 已知行为提示

- 点击 `ed2k://`、`thunder://`、`magnet:?xt=urn:btih:` 等协议链接时，浏览器会交由本机关联的下载程序处理，请确认来源可信后再点击。
- 按需求设计，正文中的 40 位十六进制字符串会被识别为磁力哈希并转换为链接；Git 提交哈希等文本也可能被转换，这不属于漏洞。

## 报告漏洞

如果你发现脚本存在安全问题（例如 XSS 风险、意外执行恶意内容），请**不要**公开披露：

1. 通过 GitHub 的私有漏洞报告功能或邮件联系维护者；
2. 在报告中描述复现步骤、受影响版本与潜在影响；
3. 维护者确认后会在修复版本发布并公开致谢后再公开展示细节。

## 支持的版本

仅维护最新发布的稳定版本（见 `CHANGELOG.md`）。安全问题修复会以补丁版本（如 1.2.1）形式发布。
